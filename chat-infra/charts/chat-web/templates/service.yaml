apiVersion: v1
kind: Service
metadata:
  name: {{ include "chat-web.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "chat-web.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
