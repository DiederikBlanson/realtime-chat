apiVersion: v1
kind: Service
metadata:
  name: {{ include "chat-infra.fullname" . }}
  labels:
    {{- include "chat-infra.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chat-infra.selectorLabels" . | nindent 4 }}
