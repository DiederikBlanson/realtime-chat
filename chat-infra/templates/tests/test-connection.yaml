apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chat-infra.fullname" . }}-test-connection"
  labels:
    {{- include "chat-infra.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chat-infra.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
