name: Test Self-Hosted Runner

on:
  push:
    branches:
      - main

jobs:
  test-runner:
    runs-on: self-hosted
    steps:
      - name: Print runner system info
        run: |
          echo "✅ Self-hosted runner is working!"
